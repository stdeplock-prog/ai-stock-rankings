name: Update Stock Rankings

# Scheduling notes
# ----------------
# We want three runs per US weekday in America/Chicago: 08:45, 12:30, 15:35.
# GitHub Actions cron runs in UTC and has no native timezone support, so for
# DST coverage we register two UTC crons per slot (CDT vs CST). To keep DST
# from doubling the work, exactly one of each twin pair must execute on any
# given calendar day; the other must exit successfully (not fail).
#
# Robustness lessons hit in earlier designs:
#   1. GitHub Actions frequently delays *or drops* scheduled runs by several
#      minutes-to-hours under load (the 13:45 UTC slot in particular was
#      dropping every weekday). Cron lines are best-effort, not guarantees.
#   2. `exit 78` in a workflow `run:` step is treated as a normal failure;
#      neutral exit codes only apply to GitHub Apps check runs, not Actions
#      steps. Skips therefore showed as red failures.
#
# Current design:
#   * Each slot has multiple cron lines per offset (primary + a long tail
#     of 15-minute backups) so a delayed/dropped firing at the primary
#     minute still has many chances. GitHub Actions has been observed to
#     delay scheduled triggers by 30-60+ minutes under load and to drop
#     entire morning slots after weekend quiet periods (May 4 2026: zero
#     scheduled deliveries between Sat 06:21Z and Mon manual dispatch),
#     so the backup window must extend well past the primary minute and
#     all the way to the slot's stale-guard MAX_HM.
#     All backup crons carry the same logical slot id; once one wins,
#     slot-level idempotency below short-circuits the rest.
#   * The "Decide whether this run should proceed" step uses
#     `github.event.schedule` (the literal cron string) to identify the
#     logical slot (morning/midday/close). Slot-level idempotency does
#     all the dedupe work, so the previous "twin offset" filter has been
#     dropped — it was rejecting valid firings on whichever DST offset
#     wasn't currently active and providing no protection that idempotency
#     doesn't already give us.
#   * Slot-level idempotency: if rankings.json's last commit on main is
#     already from today within this slot's window, subsequent firings
#     skip the heavy pipeline (proceed=false). The first cron to actually
#     succeed for the slot wins; later retries are cheap no-ops. This
#     also handles CDT/CST twin dedupe — only one of the offset pair can
#     win the slot per day.
#   * Manual `workflow_dispatch` always proceeds (overrides idempotency).
#   * No wall-clock minute matching, so GitHub Actions delays are tolerated.

on:
  schedule:
    # 08:45 America/Chicago slot — primary at 13:45 UTC plus a long
    # tail of 15-minute backups extending through 18:00 UTC (13:00 CDT).
    # The slot's stale-guard MAX_HM is 13:00 Chicago, so a firing
    # delivered as late as 13:00 CDT can still rescue the slot. May 5
    # 2026: GitHub Actions delivered zero scheduled triggers between
    # 06:33Z and 15:12Z, so morning coverage must extend well past the
    # primary minute. A long backup window is the only mitigation we
    # have for outright dropped deliveries.
    - cron: '45 13 * * 1-5'   # 08:45 CDT (primary)
    - cron: '0 14 * * 1-5'    # 09:00 CDT
    - cron: '15 14 * * 1-5'   # 09:15 CDT
    - cron: '30 14 * * 1-5'   # 09:30 CDT
    - cron: '45 14 * * 1-5'   # 09:45 CDT  [also 08:45 CST primary]
    - cron: '0 15 * * 1-5'    # 10:00 CDT / 09:00 CST
    - cron: '30 15 * * 1-5'   # 10:30 CDT / 09:30 CST
    - cron: '0 16 * * 1-5'    # 11:00 CDT / 10:00 CST
    - cron: '30 16 * * 1-5'   # 11:30 CDT / 10:30 CST
    - cron: '0 17 * * 1-5'    # 12:00 CDT / 11:00 CST  [morning rescue]
    - cron: '30 17 * * 1-5'   # 12:30 CDT (also primary midday)
    - cron: '45 17 * * 1-5'   # 12:45 CDT  [morning rescue tail]
    - cron: '0 18 * * 1-5'    # 13:00 CDT  [morning rescue tail / midday backup]
    # 12:30 America/Chicago slot.
    - cron: '15 18 * * 1-5'   # 13:15 CDT
    - cron: '30 18 * * 1-5'   # 13:30 CDT  [also 12:30 CST primary]
    - cron: '0 19 * * 1-5'    # 13:00 CST / 14:00 CDT
    - cron: '30 19 * * 1-5'   # 13:30 CST / 14:30 CDT
    # 15:35 America/Chicago slot.
    - cron: '35 20 * * 1-5'   # 15:35 CDT (primary)
    - cron: '5 21 * * 1-5'    # 16:05 CDT
    - cron: '35 21 * * 1-5'   # 16:35 CDT  [also 15:35 CST primary]
    - cron: '5 22 * * 1-5'    # 16:05 CST / 17:05 CDT
    - cron: '35 22 * * 1-5'   # 16:35 CST / 17:35 CDT
  workflow_dispatch:           # Manual trigger from Actions tab

permissions:
  contents: write
  pages: write
  id-token: write

concurrency:
  group: update-rankings-main
  cancel-in-progress: false

jobs:
  # -------------------------------------------------------
  # JOB 1: Run the data pipeline and push rankings.json
  # -------------------------------------------------------
  update-rankings:
    runs-on: ubuntu-latest

    steps:
      # Checkout up-front so the gate step can read git history for the
      # slot-level idempotency check, and the later push step has full
      # history available for rebase. fetch-depth: 0 keeps both happy.
      - name: Checkout repo
        uses: actions/checkout@v5
        with:
          fetch-depth: 0

      # Decide whether this particular trigger should run the pipeline.
      # For schedule events, only the twin that matches today's actual
      # America/Chicago UTC offset proceeds; the other exits successfully
      # so the run shows green instead of red. workflow_dispatch always
      # proceeds. Per-slot idempotency: if the slot has already been
      # satisfied today (rankings.json committed within the slot window),
      # backup firings skip. The decision is exported as `proceed` and
      # `slot` for later steps.
      - name: Decide whether this run should proceed
        id: gate
        run: |
          set -euo pipefail
          if [ "${{ github.event_name }}" != "schedule" ]; then
            echo "Manual or non-schedule trigger - proceeding."
            echo "proceed=true" >> "$GITHUB_OUTPUT"
            echo "slot=manual" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          # Current offset from UTC in America/Chicago: -0500 in CDT, -0600 in CST.
          OFFSET=$(TZ=America/Chicago date +%z)
          DOW=$(TZ=America/Chicago date +%u)   # 1=Mon..7=Sun
          TODAY_CHI=$(TZ=America/Chicago date +%Y-%m-%d)
          SCHED='${{ github.event.schedule }}'
          echo "Trigger schedule: $SCHED"
          echo "Chicago offset:   $OFFSET (dow=$DOW, today=$TODAY_CHI)"

          if [ "$DOW" -gt 5 ]; then
            echo "Weekend in Chicago - skipping (success)."
            echo "proceed=false" >> "$GITHUB_OUTPUT"
            echo "slot=weekend" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          # Map each cron to its logical slot id (morning/midday/close).
          # All cron lines in a slot's CDT+CST window map to the same SLOT,
          # and slot-level idempotency below dedupes them so only one
          # firing actually does the heavy work per slot per day.
          # Slot mapping. The morning slot extends through 18:00 UTC so a
          # late firing delivered after the midday primary can still rescue
          # a dropped morning slot via slot-level idempotency. Crons in the
          # 17:30-18:00 UTC overlap region are mapped to morning when the
          # morning slot has not yet been satisfied today, otherwise midday.
          # That dynamic decision happens after the slot-satisfied check
          # below — here we just classify the cron's primary intent.
          case "$SCHED" in
            # Morning slot crons: 13:45 UTC through 17:00 UTC.
            '45 13 * * 1-5'|'0 14 * * 1-5'|'15 14 * * 1-5'|'30 14 * * 1-5'|'45 14 * * 1-5'|'0 15 * * 1-5'|'30 15 * * 1-5'|'0 16 * * 1-5'|'30 16 * * 1-5'|'0 17 * * 1-5')
              SLOT='morning' ;;
            # Overlap crons (17:30, 17:45, 18:00 UTC): primarily midday,
            # but reclassified to morning below if morning is still unmet.
            '30 17 * * 1-5'|'45 17 * * 1-5'|'0 18 * * 1-5')
              SLOT='midday_or_morning' ;;
            # Midday slot.
            '15 18 * * 1-5'|'30 18 * * 1-5'|'0 19 * * 1-5'|'30 19 * * 1-5')
              SLOT='midday' ;;
            # Close slot.
            '35 20 * * 1-5'|'5 21 * * 1-5'|'35 21 * * 1-5'|'5 22 * * 1-5'|'35 22 * * 1-5')
              SLOT='close' ;;
            *)
              echo "Unknown schedule '$SCHED' - proceeding defensively."
              echo "proceed=true" >> "$GITHUB_OUTPUT"
              echo "slot=unknown" >> "$GITHUB_OUTPUT"
              exit 0 ;;
          esac
          NOW_HM=$(TZ=America/Chicago date +%H:%M)

          # For overlap crons (12:30/12:45/13:00 CDT, 11:30/11:45/12:00 CST),
          # check whether today's morning slot is still unmet — if so, treat
          # this firing as a morning rescue. Otherwise it's a normal midday
          # firing.
          LAST=$(git log -1 --format=%ct -- data/rankings.json 2>/dev/null || echo "")
          LAST_DATE=""
          LAST_HM=""
          if [ -n "$LAST" ]; then
            LAST_DATE=$(TZ=America/Chicago date -d "@$LAST" +%Y-%m-%d)
            LAST_HM=$(TZ=America/Chicago date -d "@$LAST" +%H:%M)
          fi
          if [ "$SLOT" = "midday_or_morning" ]; then
            # Morning unmet today if no commit today, or last commit before 08:45.
            if [ -z "$LAST" ] || [ "$LAST_DATE" != "$TODAY_CHI" ] || [ "$LAST_HM" \< "08:45" ]; then
              SLOT='morning'
              echo "Overlap cron $SCHED: morning slot unmet today, rescuing as morning."
            else
              SLOT='midday'
              echo "Overlap cron $SCHED: morning satisfied, treating as midday."
            fi
          fi
          echo "slot=$SLOT" >> "$GITHUB_OUTPUT"

          # Stale-firing guard. Each slot has a wall-clock window in Chicago
          # time outside which the firing is treated as too stale to act on.
          # The morning MAX_HM extends to 13:00 so a late delivery (GitHub
          # Actions has dropped entire morning windows — see May 5 2026)
          # can still rescue the slot via the overlap crons up to 13:00 CDT.
          case "$SLOT" in
            morning) MIN_HM='08:45'; MAX_HM='13:00' ;;
            midday)  MIN_HM='12:30'; MAX_HM='15:00' ;;
            close)   MIN_HM='15:35'; MAX_HM='18:30' ;;
            *)       MIN_HM='00:00'; MAX_HM='23:59' ;;
          esac
          # Lexicographic compare on zero-padded HH:MM is safe.
          if [ "$NOW_HM" \< "$MIN_HM" ] || [ "$NOW_HM" \> "$MAX_HM" ]; then
            echo "Firing outside $SLOT window ($NOW_HM not in [$MIN_HM,$MAX_HM]) - skipping stale (success)."
            echo "proceed=false" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          # Slot-level idempotency.
          if [ -z "$LAST" ]; then
            echo "No prior rankings.json commit found - proceeding."
            echo "proceed=true" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          echo "Last rankings.json commit (Chicago): $LAST_DATE $LAST_HM"

          if [ "$LAST_DATE" != "$TODAY_CHI" ]; then
            echo "No commit today yet - proceeding."
            echo "proceed=true" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          # A commit at or after MIN_HM today satisfies the slot. No upper
          # bound on the satisfying commit (a later same-day commit always
          # counts as having covered the slot).
          if [ "$LAST_HM" \< "$MIN_HM" ]; then
            echo "Today's $SLOT slot not yet satisfied (last commit $LAST_HM < $MIN_HM) - proceeding."
            echo "proceed=true" >> "$GITHUB_OUTPUT"
          else
            echo "Today's $SLOT slot already satisfied (last commit $LAST_HM >= $MIN_HM) - skipping (success)."
            echo "proceed=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Set up Python 3.11
        # Set up Python on every firing — we need it even when proceed=false
        # so that skip records can be appended to workflow_runs.jsonl.
        uses: actions/setup-python@v6
        with:
          python-version: '3.11'

      # Skip-path runs are no-ops: we no longer write a JSONL record or a
      # commit for them. Each skip-record commit was producing repo noise
      # (one push per stale firing) without giving the reliability report
      # actionable signal — the run-history is already visible in the
      # GitHub Actions run list and in proceed-path JSONL entries. Earlier
      # designs committed skip records on every firing; that produced 19+
      # `[auto] schedule reliability skip record` commits in a few hours
      # on May 4-5 2026 with no recovery benefit.

      - name: Install dependencies
        if: steps.gate.outputs.proceed == 'true'
        run: |
          pip install --upgrade pip
          pip install pandas numpy requests yfinance scipy ta scikit-learn python-dotenv openpyxl lxml

      - name: Create required data directories
        if: steps.gate.outputs.proceed == 'true'
        run: |
          mkdir -p data/reference
          mkdir -p data/raw/ohlcv_daily
          mkdir -p data/processed/technical_indicators
          mkdir -p data/processed/scoring_outputs

      - name: Build universe (S&P 500 + Nasdaq 100)
        if: steps.gate.outputs.proceed == 'true'
        run: python 02_Code/Python/Data_Fetch/build_universe.py

      - name: Fetch OHLCV + fundamentals
        if: steps.gate.outputs.proceed == 'true'
        run: python 02_Code/Python/Data_Fetch/fetch_ohlcv.py
        env:
          EODHD_API_KEY: ${{ secrets.EODHD_API_KEY }}

      - name: Calculate technical indicators
        if: steps.gate.outputs.proceed == 'true'
        run: python 02_Code/Python/Indicators/calc_indicators.py

      - name: Score tickers
        if: steps.gate.outputs.proceed == 'true'
        run: python 02_Code/Python/Scoring_Engine/score_tickers.py

      - name: Fetch catalysts (earnings + analyst ratings)
        if: steps.gate.outputs.proceed == 'true'
        run: python 02_Code/Python/Data_Fetch/fetch_catalysts.py
        env:
          DATA_PROVIDER: ${{ vars.DATA_PROVIDER || 'yfinance' }}
          EODHD_API_KEY: ${{ secrets.EODHD_API_KEY }}

      - name: Score swing (SwingScore + tiers)
        if: steps.gate.outputs.proceed == 'true'
        run: python 02_Code/Python/Scoring_Engine/score_swing.py

      - name: Export rankings to JSON
        if: steps.gate.outputs.proceed == 'true'
        run: python 02_Code/Python/Scoring_Engine/export_to_json.py

      # EODHD live calls are gated by EODHD_ENRICHMENT_ENABLED, only set on
      # the morning slot or a manual dispatch so live fundamentals fetches
      # happen at most once per ticker per day. Midday/close runs read the
      # on-disk cache populated by the morning run (see
      # 02_Code/Python/Data_Fetch/eodhd_fundamentals.py: use_cache
      # short-circuits before the API key check) and rely on EODHD_MAX_
      # FUNDAMENTAL_CALLS=0 to refuse any uncached live call.
      #
      # The API key itself is injected unconditionally — earlier we tried
      # ${{ (cond) && secrets.X || '' }} on the env line and the watchlist
      # step received an empty string in production despite the env block
      # showing "EODHD_API_KEY: ***" in the logs (run 25336931497, 2026-05-04
      # workflow_dispatch: zero live_calls/cache_hits/deferred for 12 supp
      # equities). Conditional expressions on env values are fragile when
      # combined with secret masking; gating via a plain boolean instead
      # keeps the secret reference unambiguous and the gate observable.
      - name: Generate watchlist rankings JSON (CSV + TradingView union)
        if: steps.gate.outputs.proceed == 'true'
        run: python 02_Code/Python/Scoring_Engine/generate_watchlist_rankings.py
        continue-on-error: true
        env:
          EODHD_API_KEY: ${{ secrets.EODHD_API_KEY }}
          # Boolean gate replaces the previous conditional-secret pattern.
          # When 'false', the script forces EODHD_MAX_FUNDAMENTAL_CALLS=0 so
          # no live API traffic is generated, regardless of whether the key
          # is set. Cache hits remain free.
          EODHD_ENRICHMENT_ENABLED: ${{ (github.event_name == 'workflow_dispatch' || steps.gate.outputs.slot == 'morning') && 'true' || 'false' }}
          # Hard cap on live EODHD fundamentals calls per run. Cache hits are
          # always allowed; this only bounds network traffic. Sized for the
          # free EODHD plan's daily fundamentals quota with headroom for
          # fetch_ohlcv.py + fetch_catalysts.py, which also hit EODHD on the
          # morning slot.
          EODHD_MAX_FUNDAMENTAL_CALLS: '15'

      # Daily reports: only generate on the morning slot (08:45 America/Chicago)
      # or on manual dispatch. Midday and close runs skip these so the reports
      # stay stable to the morning snapshot. The slot-level idempotency above
      # ensures at most one of the morning crons does heavy work per day.
      - name: Generate market risk monitor report
        if: >-
          steps.gate.outputs.proceed == 'true' &&
          (github.event_name == 'workflow_dispatch' ||
           steps.gate.outputs.slot == 'morning')
        run: python 02_Code/Python/Reports/market_risk_monitor.py
        continue-on-error: true

      - name: Generate options/earnings watchlist report
        if: >-
          steps.gate.outputs.proceed == 'true' &&
          (github.event_name == 'workflow_dispatch' ||
           steps.gate.outputs.slot == 'morning')
        run: |
          pip install markdown
          python 02_Code/Python/Reports/options_watchlist.py
        continue-on-error: true

      # Run after all artifact-producing steps so the audit sees the freshest
      # rankings, watchlist, and tasks state. continue-on-error: a failed
      # audit must not block the rankings commit.
      - name: Generate data quality audit
        if: steps.gate.outputs.proceed == 'true'
        run: python 02_Code/Python/Reports/data_quality_audit.py
        continue-on-error: true

      # Scoring Parity Review — compares score semantics and coverage across
      # main rankings, watchlist main_pipeline rows, and watchlist SUPP rows.
      # Reads only the published rankings/watchlist JSON, no network. Always
      # safe to run; continue-on-error so a transient parsing failure cannot
      # block the rankings commit.
      - name: Generate scoring parity review
        if: steps.gate.outputs.proceed == 'true'
        run: python 02_Code/Python/Reports/scoring_parity_review.py
        continue-on-error: true

      # Low-Risk Drift Review — diagnostic report that explains *why* the
      # parity review's low_risk gap exists (selection bias vs data gap vs
      # formula issue). Reads scoring_parity_review.json for context, so it
      # must run AFTER the parity review. Read-only, no network. Runs before
      # the midday health check so both can read its JSON output.
      - name: Generate low-risk drift review
        if: steps.gate.outputs.proceed == 'true'
        run: python 02_Code/Python/Reports/low_risk_drift_review.py
        continue-on-error: true

      # Benchmark Review — model-validation report comparing leader buckets
      # against momentum/trend, sector concentration, SPY/QQQ/IWM context,
      # and a forward-performance snapshot scaffold. Reads existing artifacts
      # plus yfinance for the market ETFs only (no EODHD calls), so it is
      # safe to run on every slot. continue-on-error so a transient yfinance
      # failure cannot block the commit.
      - name: Generate benchmark review
        if: steps.gate.outputs.proceed == 'true'
        run: python 02_Code/Python/Reports/benchmark_review.py
        continue-on-error: true

      # Record the run on the proceed path before the reliability report is
      # generated so the report sees the current run in its JSONL input.
      # rankings.json was just rewritten by the export step above, so we
      # can pull as_of/open_date directly from it.
      - name: Record run gate (proceed path)
        if: steps.gate.outputs.proceed == 'true'
        env:
          SR_EVENT_NAME: ${{ github.event_name }}
          SR_SLOT: ${{ steps.gate.outputs.slot }}
          SR_PROCEEDED: 'true'
          SR_RUN_URL: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
          SR_COMMIT_SHA: ${{ github.sha }}
        run: |
          set -e
          AS_OF=$(python -c "import json,sys; d=json.load(open('data/rankings.json')); print(d.get('as_of','') or '')" 2>/dev/null || echo "")
          OPEN_DATE=$(python -c "import json,sys; d=json.load(open('data/rankings.json')); print(d.get('open_date','') or '')" 2>/dev/null || echo "")
          export SR_AS_OF="$AS_OF"
          export SR_OPEN_DATE="$OPEN_DATE"
          python 02_Code/Python/Reports/schedule_reliability.py --record-run

      # Schedule reliability report — reads workflow_runs.jsonl plus the
      # current rankings.json to produce a calendar of recent slot deliveries
      # and freshness signals. continue-on-error keeps a transient failure
      # from blocking the rankings commit.
      - name: Generate schedule reliability report
        if: steps.gate.outputs.proceed == 'true'
        run: python 02_Code/Python/Reports/schedule_reliability.py
        continue-on-error: true

      # Ranking Diagnostics — explanation layer over the published
      # leaders. Reads rankings/watchlist plus the sibling diagnostic
      # JSONs (data quality, scoring parity, low-risk drift, benchmark)
      # to flag suspicious top ranks, sector crowding, and run alternate
      # weighting sensitivity. Read-only; no scoring changes. Must run
      # AFTER all the upstream diagnostic JSONs are written so it can
      # cite their verdicts. continue-on-error so a transient failure
      # cannot block the rankings commit.
      - name: Generate ranking diagnostics
        if: steps.gate.outputs.proceed == 'true'
        run: python 02_Code/Python/Reports/ranking_diagnostics.py
        continue-on-error: true

      # Market Open Scan — morning briefing rollup. Like the midday
      # health check, it consumes the other reports' JSON artifacts and
      # runs on every proceeded slot so the dashboard reflects the current
      # operational state. Must run AFTER data quality / schedule reliability
      # / market risk / benchmark reports have written their JSON, but BEFORE
      # the midday health check (the briefing is part of the morning health
      # picture). continue-on-error so a transient failure cannot block the
      # commit.
      - name: Generate market open scan
        if: steps.gate.outputs.proceed == 'true'
        run: python 02_Code/Python/Reports/market_open_scan.py
        continue-on-error: true

      # Midday Health Check — operational rollup that consumes the other
      # reports' artifacts and writes a single OK/WARN/FAIL summary plus
      # action items. Runs on EVERY proceeded slot (not just morning) so
      # the dashboard's task table reflects the current state of the
      # pipeline rather than the morning snapshot. Must run last so the
      # other reports' JSONs already exist on disk when it reads them.
      - name: Generate midday health check
        if: steps.gate.outputs.proceed == 'true'
        run: python 02_Code/Python/Reports/midday_health_check.py
        continue-on-error: true

      # Close Recap — end-of-day rollup. Consumes rankings, watchlist, and
      # the other reports' JSON artifacts (including market_open_scan for
      # the morning top10 baseline so it can flag intraday additions).
      # Like the other rollups it runs on EVERY proceeded slot, but is
      # most useful at/after the 3:35 PM CT close refresh — the task row
      # advertises it as the close report. Must run AFTER market_open_scan
      # and the other diagnostic JSONs are written.
      - name: Generate close recap
        if: steps.gate.outputs.proceed == 'true'
        run: python 02_Code/Python/Reports/close_recap.py
        continue-on-error: true

      - name: Commit and push updated data files
        if: steps.gate.outputs.proceed == 'true' && github.ref == 'refs/heads/main'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add data/rankings.json
          git add data/watchlist_rankings.json 2>/dev/null || true
          git add data/watchlist_sources.json 2>/dev/null || true
          git add data/processed/scoring_outputs/rankings_daily_open.csv 2>/dev/null || true
          git add data/processed/scoring_outputs/rankings_daily_open_date.txt 2>/dev/null || true
          git add data/processed/catalysts.csv 2>/dev/null || true
          git add data/processed/scoring_outputs/swing_rankings.csv 2>/dev/null || true
          git add data/reference/master_universe.csv 2>/dev/null || true
          git add data/reports/market_risk_monitor.json 2>/dev/null || true
          git add data/reports/data_quality_audit.json 2>/dev/null || true
          git add data/reports/benchmark_review.json 2>/dev/null || true
          git add data/reports/benchmark_snapshots.jsonl 2>/dev/null || true
          git add data/reports/scoring_parity_review.json 2>/dev/null || true
          git add data/reports/low_risk_drift_review.json 2>/dev/null || true
          git add data/reports/ranking_diagnostics.json 2>/dev/null || true
          git add data/reports/schedule_reliability.json 2>/dev/null || true
          git add data/reports/market_open_scan.json 2>/dev/null || true
          git add data/reports/midday_health_check.json 2>/dev/null || true
          git add data/reports/close_recap.json 2>/dev/null || true
          git add data/reports/workflow_runs.jsonl 2>/dev/null || true
          git add data/cache/yfinance_info 2>/dev/null || true
          git add reports/market-risk-monitor.html 2>/dev/null || true
          git add reports/options-watchlist.html 2>/dev/null || true
          git add reports/data-quality-audit.html 2>/dev/null || true
          git add reports/benchmark-review.html 2>/dev/null || true
          git add reports/scoring-parity-review.html 2>/dev/null || true
          git add reports/low-risk-drift-review.html 2>/dev/null || true
          git add reports/ranking-diagnostics.html 2>/dev/null || true
          git add reports/schedule-reliability.html 2>/dev/null || true
          git add reports/market-open-scan.html 2>/dev/null || true
          git add reports/midday-health-check.html 2>/dev/null || true
          git add reports/close-recap.html 2>/dev/null || true
          # tasks.json is stamped by the daily report scripts (last_run /
          # next_run / status / summary). If we don't stage it here, the later
          # `git pull --rebase` aborts with "unstaged changes".
          git add data/tasks.json 2>/dev/null || true

          if git diff --staged --quiet; then
            echo "No changes to commit."
            exit 0
          fi

          git commit -m "[auto] Update rankings $(date -u '+%Y-%m-%d %H:%M') UTC"
          git fetch origin main
          git pull --rebase origin main
          git push origin HEAD:main

    outputs:
      proceeded: ${{ steps.gate.outputs.proceed }}

  # -------------------------------------------------------
  # JOB 2: Deploy site to GitHub Pages (runs after Job 1)
  # Only deploy when the pipeline actually ran; otherwise the
  # gated-out twin would redeploy unchanged content (or fail).
  # -------------------------------------------------------
  deploy-pages:
    if: github.ref == 'refs/heads/main' && needs.update-rankings.outputs.proceeded == 'true'
    needs: update-rankings
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: Checkout repo (with fresh data)
        uses: actions/checkout@v5
        with:
          ref: main

      - name: Upload site artifact
        uses: actions/upload-pages-artifact@v5
        with:
          path: '.'

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5
